# Database Schema

This page moved. The canonical, code-verified schema reference is
[SCHEMA.md](SCHEMA.md), which mirrors `src/codebase_index/storage/schema.sql`
(tables `files`, `symbols`, `chunks`, `edges` with a `confidence` column, `modules`,
`meta`, the `fts_chunks` FTS5 table, and the opt-in `vec_*` tables).
